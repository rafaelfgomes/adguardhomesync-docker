origin:
  url: ${ADGUARDHOME_ORIGIN_URL}
  username: ${ADGUARDHOME_ORIGIN_USERNAME}
  password: ${ADGUARDHOME_PASSWORD}
replicas:
  - url: ${ADGUARDHOME_REPLICA_URL}
    username: ${ADGUARDHOME_REPLICA_USERNAME}
    password: ${ADGUARDHOME_PASSWORD}
cron: "*/5 * * * *" # Sync every 5 minutes
